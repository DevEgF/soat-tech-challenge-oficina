package com.soat.tech.challenge.oficina.presentation

/**
 * Camada Interface (apresentação): controllers REST, contratos HTTP e validação de entrada.
 * O pacote chama-se `presentation` porque `interface` é palavra reservada em Kotlin/Java.
 */
internal object PresentationLayer
